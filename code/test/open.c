/* readString.c
 *
 *	Simple program to test reading a string
 *
 */

#include "syscall.h"

int main()
{
  // char str[256];
  // ReadString(str, 255);
  // PrintString("\nRead string is: ");
  // PrintString(str);
  // PrintString("\n");
  // Create("hung.txt");
  Open("hung.txt");
  // PrintNum(id);
  Halt();
  /* not reached */
}
